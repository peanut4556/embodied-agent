# Used later by the RAI-backed planner. The mock demo needs no secrets.
OPENAI_API_KEY=

# Select mock, ros2, or lerobot when the corresponding adapter is configured.
EMBODIED_AGENT_MODE=mock
ROS_DOMAIN_ID=42

